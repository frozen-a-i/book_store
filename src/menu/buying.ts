import { Menu } from "@grammyjs/menu";
import { CARD_NUMBER } from "../constants";
import { checkImage } from "./checkImage";

// export const makeOrder = new Menu("make-order").text("Sotib olish", async (ctx) => {
//   await ctx.reply(`To'lovni ushbu karta raqamiga yuboring! ${CARD_NUMBER}`, {
//     reply_markup: checkImage,
//   });
// });
